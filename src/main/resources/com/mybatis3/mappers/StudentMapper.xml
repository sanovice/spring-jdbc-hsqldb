<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE cofiguration 
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.mybatis3.mappers.StduentMapper">
	<resultMap type="Student" id="StudentResult">
		<id property="studId" column="stud_id"/>
		<result property="name" column="name"/>
		<result property="email" column="email"/>
		<resutl property="dob" column="dob"/>
	</resultMap>
	
	<select id="findAllStudents" resultMap="StudentResult">
		select * from students
	</select>
	
	<select id="findStudentById" parameterType="int" resultType="Student">
		select stud_id as studid, nae, email, dob
		  from students where stud_id=#{Id}
	</select>
	
	<insert id="isertStudent" parameterType="Student">
		insert into student(stud_id, name, email, dob)
		  values (#{studId}, #{name}, #{email}, #{dob})
	</insert>
</mapper>